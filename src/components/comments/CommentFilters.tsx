'use client'

import React from 'react'

interface CommentFiltersProps {
  // Composant désactivé - pas de tri
}

export function CommentFilters(props: CommentFiltersProps) {
  // Composant retiré - retourne null pour ne rien afficher
  return null
}